# Test Data Directory

This directory contains generated test log files (not tracked in git).

## Generate Test Data

```bash
cd ../generate_logs
cargo build --release

# Apache logs (125MB per 1M lines)
./target/release/generate_logs -o ../data/apache_1M.log -l 1000000 -f apache

# JSON logs (180MB per 1M lines)
./target/release/generate_logs -o ../data/json_1M.log -l 1000000 -f json

# Syslog (75MB per 1M lines)
./target/release/generate_logs -o ../data/syslog_1M.log -l 1000000 -f syslog
```
